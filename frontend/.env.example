# Base URL of the FastAPI backend (src/api). Defaults to http://127.0.0.1:8000
# if unset - only needed if the backend runs somewhere else (e.g. through
# Docker Compose on a different host/port). Copy to `.env.local` to override.
VITE_API_BASE_URL=http://127.0.0.1:8000
